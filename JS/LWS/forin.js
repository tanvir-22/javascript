const myobj ={
    name:"java",
    estd:'1991',
    owner:'james gosling'
}
for(property in myobj){ //for in
    console.log(property);
}
const myarray = [33,55,32,66];
for(element of myarray){    //for of 
    console.log(element);
}